timeseries space_bytes = avg(purefa_array_space_bytes),
           by: { dt.entity.pure_storage_flash_array },
           from: -24h
| summarize current_space_bytes = last(space_bytes),
            by: { dt.entity.pure_storage_flash_array }
| sort current_space_bytes desc
