import { credentialVaultClient } from "@dynatrace-sdk/client-classic-environment-v2";
export default async function () {
  const sec = await credentialVaultClient.getCredentialById({ id: "credentials_vault-312312" });
  return { markdownEmail: `VAULT_OK ${Object.keys(sec || {}).join(",")}` };
}
