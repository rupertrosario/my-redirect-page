const cred = workflow.credential("cohesity-helios-api-key");
const apiKey = cred?.value || cred?.token || cred?.password;
log.info(`workflow object: ${typeof workflow}, http: ${typeof http}, apiKey loaded: ${!!apiKey}`);
return { ok: !!apiKey };
