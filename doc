Cohesity PROD Backup Failures Workflow
Dynatrace workflow monitors backup failures across all Cohesity clusters, limited to production protection groups (PROD), by identifying only objects with failed backups that do not have any subsequent successful backup and capturing object-level details. The workflow runs every 4 hours and sends email reports showing only unresolved failures for production workloads.
