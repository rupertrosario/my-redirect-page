**Progress update:**

* Created the Policy-to-PG Retention Alignment report using read-only Cohesity Helios API calls.
* Added validation for PROD, MOD/NONPROD/CAP, DEV, and LogShipping naming alignment.
* Excluded default policies: `Protect Once`, `Silver`, `Gold`, `Bronze`.
* Added exception-only output with summary counts for policies, PGs, OK items, and exceptions.
* Created the Policy Inventory / Summary report in UI-style format with backup, retention, DataLock, quiet times, retry, log backup, replication, and PG count.
* Converted inventory output to compact Dynatrace Markdown table format for email.

**Next steps:**

* Test both reports against PROD clusters.
* Validate alignment exceptions and PG counts against Cohesity UI.
* Review output with the team and confirm final naming/retention rules.
* Finalize Dynatrace email formatting.
* Attach sample outputs to JIRA and schedule both workflows.
