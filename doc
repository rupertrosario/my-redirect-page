Dynatrace workflow monitors backup failures across all Cohesity clusters over a continuous 24-hour cycle at 4-hour intervals, identifying only objects with failed backups that do not have any subsequent successful backup and capturing object-level details. The workflow creates one incident per day for the cycle and automatically updates the incident every 4 hours with the latest unresolved failures. The assigned user provides manual updates and closes the incident upon resolution.
