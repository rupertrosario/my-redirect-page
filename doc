Create a baseline list of servers that are excluded from backup validation and categorize them by exclusion type, such as AD/Domain Controllers, Exchange, AWS, or other approved categories.
Use the existing Confluence guidance and referenced SharePoint list as source inputs.
The final list should support validation of “No Backup / No Object” results from the backup validation script and help determine whether remediation or owner confirmation is required.
