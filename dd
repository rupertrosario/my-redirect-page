# --- Prompt for credentials ---
$cred = Get-Credential   # popup for username/password

# --- Ignore SSL (self-signed certs) ---
add-type @"
using System.Net;
using System.Security.Cryptography.X509Certificates;
public class TrustAllCertsPolicy : ICertificatePolicy {
    public bool CheckValidationResult(ServicePoint srvPoint, X509Certificate certificate,
    WebRequest request, int certificateProblem) { return true; }
}
"@
[System.Net.ServicePointManager]::CertificatePolicy = New-Object TrustAllCertsPolicy

# --- GET system info ---
$ddIP = "https://<dd-ip>"
$response = Invoke-RestMethod -Uri "$ddIP/rest/v1/system" -Method GET -Credential $cred

$response


# --- Prompt credentials ---
$cred = Get-Credential

# --- SSL bypass for PS 5.1 ---
[System.Net.ServicePointManager]::ServerCertificateValidationCallback = { $true }

# --- Test base connectivity ---
$dd = "https://<dd-ip>"

Invoke-WebRequest -Uri $dd -Method GET -Credential $cred

# --- Test API path (adjust if 404) ---
Invoke-RestMethod -Uri "$dd/rest/v1/system" -Method GET -Credential $cred
